name: charts-mainwaring-dev
region: nyc
static_sites:
  - environment_slug: html
    name: charts-mainwaring-dev
    github:
      branch: master
      repo: theaccordance/mainwaring_dev
    routes:
      - path: /
    source_dir: /client/build
    build_command: cd ../ && npm run build
