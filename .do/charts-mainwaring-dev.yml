name: charts-mainwaring-dev
region: nyc
static_sites:
  - environment_slug: html
    git:
      branch: master
      repo_clone_url: https://github.com/theaccordance/chartbrew.git
    name: charts-mainwaring-dev
    routes:
      - path: /
    source_dir: /client/build
    build_command: cd client && npm run build
