name: charts-mainwaring-dev
region: nyc
static_sites:
  - environment_slug: html
    name: charts-mainwaring-dev
    github:
      branch: master
      repo: theaccordance/chartbrew
    routes:
      - path: /
    build_command: npm run ci && cd client && npm run build
    source_dir: /
    output_dir: /client/build

